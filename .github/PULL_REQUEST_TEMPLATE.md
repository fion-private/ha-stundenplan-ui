## What does this PR do?

<!-- Describe the change and, if applicable, which issue it fixes (e.g. "Fixes #12"). -->

## Checklist

- [ ] Tests added/updated for the change (`tests/`)
- [ ] `npm run lint` and `npm run format` pass locally
- [ ] `npm run typecheck` passes locally
- [ ] `npm test` passes locally
- [ ] `npm run build` succeeds locally
- [ ] Documentation (`README.md`) updated if user-facing behavior changed
- [ ] New user-facing strings added to **both** `src/localize/languages/en.json` and `de.json`
